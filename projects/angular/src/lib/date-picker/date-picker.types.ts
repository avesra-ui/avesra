import type { AvDateFieldValue } from '../date-field/date-field.types';

/** Selected value for DatePicker (same as DateField). */
export type AvDatePickerValue = AvDateFieldValue;

export type { AvDateFieldValue };
