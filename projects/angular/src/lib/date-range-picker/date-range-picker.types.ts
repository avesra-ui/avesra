import type { AvRangeCalendarValue } from '../range-calendar/range-calendar.types';

/** Selected value for DateRangePicker (complete ranges only). */
export type AvDateRangePickerValue = AvRangeCalendarValue;

export type { AvRangeCalendarValue };
